{
  "name": "plexus-engine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://philofobreal.github.io/plexus-engine/",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@types/p5": "^1.7.7",
    "gh-pages": "^6.3.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "p5": "^2.2.3"
  }
}